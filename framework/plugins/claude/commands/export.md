---
description: Export the current model to an Excel workbook.
---

1. Run `expression export` from the workspace root.
2. Report the path of the generated `.xlsx`.
3. If round-trip verification reports differences, surface the cells that
   diverged — do not silently swallow them.
